export interface ICar {
  plate: string;
  model: string;
  brand: string;
  current_km: number;
  vehicle_year: Date;
}

export interface ICarResponse {
  plate: string;
  model: string;
  brand: string;
  current_km: number;
  vehicle_year: Date;
}
